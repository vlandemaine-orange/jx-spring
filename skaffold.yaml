apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/vlandemaine-orange/jx-spring:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/vlandemaine-orange/jx-spring:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: jx-spring
        chartPath: charts/jx-spring
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/vlandemaine-orange/jx-spring'
          image.tag: '{{.DIGEST_HEX}}'
